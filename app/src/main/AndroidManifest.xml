<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.shoptools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.pedido.item_pedidoActivity"/>
        <activity android:name=".ui.pedido.AddPedActivity" />
        <activity android:name=".ui.produtos.AddProdActivity" />
        <activity android:name=".ui.cliente.AddCliActivity" />
        <activity android:name=".ui.configuracoes.ConfigActivity" />
        <activity android:name=".ui.pedido.PedidoActivity" />
        <activity android:name=".ui.produtos.ProdutoActivity" />
        <activity android:name=".ui.cliente.ClienteActivity" />
    </application>

</manifest>